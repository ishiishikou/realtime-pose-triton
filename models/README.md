# Model artifacts

Model files are not committed to this repository.

Place a compatible ONNX artifact at:

```text
models/rtmpose/1/model.onnx
```

or set `RTMPOSE_ONNX_URL` to download the artifact when the Triton container starts.
